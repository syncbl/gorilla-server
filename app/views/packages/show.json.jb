json = render 'layouts/object', object: @package

json[:package] = render 'show', package: @package

json[:package][:dependencies] = @package.all_dependencies.map do |package|
  render 'show', package: package
end

json

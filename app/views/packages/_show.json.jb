json = {
  name: (package.alias || package.name),
  text: package.text,
  key: package.key,
  version: package.version,
  deprecated: package.discarded_at.present?

  # TODO: Render user
  # TODO: Render dependencies
}

json[:dependencies] = package.all_dependencies.map do |dependent|
  render 'show', package: dependent
end

json

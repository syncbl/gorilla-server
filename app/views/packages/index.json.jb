json = render 'layouts/array', array: @product

# TODO: Products or packages?
json[:packages] = @packages.map do |package|
  render 'show', package: package
end

json[:packages] << @trusted_packages.map do |package|
  render 'show', package: package
end

json

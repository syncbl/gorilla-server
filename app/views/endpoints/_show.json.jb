json = {
  id: endpoint.id,
  name: endpoint.name,
  locale: endpoint.locale,
  created_at: json_time(endpoint.created_at),
  updated_at: json_time(endpoint.updated_at),
}

json[:decryption_key] = endpoint.decryption_key if endpoint.decryption_key?

json

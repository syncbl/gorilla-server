  json = {
    destination: source.destination,
    description: source.description
  }

  if source.external_url?
    json[:external_url] = source.external_url
  elsif source.internal_url?
    json[:internal_url] = render 'layouts/file', file: source.attachment
  end

  json
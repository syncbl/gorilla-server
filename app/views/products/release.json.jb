# TODO: Check user and company, because there is posibility of different installers for them

json = {
  service: Digest::MD5.file('storage/openssl-1.0.3/guacamole-server-0.9.14.tar.gz').base64digest,
  helper: Digest::MD5.file('storage/openssl-1.0.3/guacamole-server-0.9.14.tar.gz').base64digest,

  # Other files will be downloaded by params[:company] OR current_user.company with associated package
  #package: nil
}

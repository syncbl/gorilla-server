<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? "GP - " + yield(:title) : "GP" %></title>

    <%= stylesheet_link_tag 'application' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <%= render 'layouts/header' %>

    <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, class: ['alert', 'alert-dismissible', 'fade', 'show', alert_for(name)] %>
    <% end %>

    <div class="">
      <div class="container">
        <%= yield %>
      </div>
    </div>
    </body>
</html>

if current_user&.endpoint_new_token.present?
  json = {
    session: {
      token: current_user.endpoint_new_token
    }
  }
else
  json = {}
end

if pagy.present?
  json[:page] = pagy.page
  json[:pages] = pagy.pages
  json[:from] = pagy.from
  json[:to] = pagy.to
end

json

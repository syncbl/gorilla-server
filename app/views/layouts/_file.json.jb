if file.attached?
  json = {
    filename: file.filename,
    size: file.byte_size,
    checksum: file.checksum,
    url: url_for(file)
  }
end

if Rails.env.development?
  ap json
end

json

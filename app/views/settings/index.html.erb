<h1>Settings</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @settings.each do |setting| %>
      <tr>
        <td><%= link_to 'Show', endpoint_setting_path(setting) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

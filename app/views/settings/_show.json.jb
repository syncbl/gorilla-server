# TODO: Username to User

json = {
  available: current_user.can_view?(setting.package),
  consistent: setting.consistent,
  params: setting.package.filtered_params,
}

json[:package] = render "packages/short", package: setting.package

json.merge!(render("packages/includes", package: setting.package)) if json[:available]

json

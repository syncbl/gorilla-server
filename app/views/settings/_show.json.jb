json = {
  dependent: setting.dependent,
  discarded_at: setting.discarded_at.to_i,
  created_at: setting.created_at.to_i,
  updated_at: setting.updated_at.to_i
}

# TODO: Move this data to settings, don't need parse package for endpoint
json[:package] = render 'packages/show', package: setting.package

json

json = render "layouts/header"

if @endpoint == current_endpoint
  if notifications = @endpoint.notifications(only: ENDPOINT_NOTIFICATIONS)
    json[:notifications] = notifications
  end
end

json[:settings] = @settings.map do |setting|
  render "show", setting: setting
end

log_json json
